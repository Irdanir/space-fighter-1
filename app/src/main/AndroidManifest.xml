<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.stepanov.space_fighter_alpha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="ru.stepanov.space_fighter_alpha.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ru.stepanov.space_fighter_alpha.GameView">
            <intent-filter>
                <action android:name="ru.pavlenty.surfacegame2.GameActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>